import { product } from './product'
import { category } from './category'
import { brand } from './brand'
import { storeSettings } from './storeSettings'

export const schemaTypes = [product, category, brand, storeSettings]